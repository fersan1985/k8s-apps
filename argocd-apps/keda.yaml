apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keda
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "1"  # Wave 1: Platform components first
spec:
  project: default

  source:
    repoURL: https://kedacore.github.io/charts
    chart: keda
    targetRevision: 2.13.1
    helm:
      releaseName: keda
      valueFiles:
        - $values/platform/keda/values.yaml

  sources:
    - repoURL: https://kedacore.github.io/charts
      chart: keda
      targetRevision: 2.13.1
    - repoURL: https://github.com/fersan1985/k8s-apps.git
      targetRevision: HEAD
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: keda

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true  # Fix for large CRD annotations
      - Replace=true  # Force replace instead of patch
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
