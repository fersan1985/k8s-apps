apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-registry-storage
  namespace: docker-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi  # Ajustar según necesidades
  storageClassName: local-path  # Ajustar según tu storageClass
