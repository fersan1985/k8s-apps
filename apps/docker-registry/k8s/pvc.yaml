apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-registry-storage
  namespace: docker-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi  # Space for Docker images
  storageClassName: rook-ceph-block  # Using Rook-Ceph storage
